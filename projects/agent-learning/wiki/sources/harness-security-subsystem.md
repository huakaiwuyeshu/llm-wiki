# Harness · 企业落地的安全底座(六子系统图)

> TL;DR:Harness 是包在 Agent Loop 决策核心外圈的"运行时护甲",由六个子系统组成,按"不崩/不闯祸/看得见"三问归类;上线红线 = 工具治理 + 安全防护 + 可观测性三件套,错误恢复/迭代控制/上下文工程可随流量迭代。

## 元数据
- 原文件:`D:\zuhaowan-ai\llm-wiki\sources\Harness安全底座-子系统图.html`
- 文档类型:图解(HTML 专题拆解图:包裹示意 + 六卡片 + 评审检查清单 + 优先级)。**疑似用户自撰**内部学习稿(自述"内部学习稿 · Harness 专题拆解",页脚"设计应用 UI/UX Pro Max skill")
- 配套关系:自述为《Agent 演进图 · 深度注解版》第 ⑧ 阶段 Harness 的放大图;体系对齐《从0到1搭建 Agent》(阿里技术);多处映射《企业业务 Agent 模块解析手册》模块编号
- ingest 日期:2026-06-10

## 核心内容

### 为什么单独画(Thesis)
Demo 能跑 ≠ 能上线。LLM 工具调用格式错误率 **5–10%**,幻觉更高——没有护甲,Agent 撑不过 24 小时真实流量。对 PM 而言,Harness 对应手册里的 Audit、工具治理、人工审核、容错重试,合起来 = 一套企业版 Harness。

### 01 架构位置:包裹示意
图:虚线外壳「HARNESS · 运行时护甲」包住内核「Agent Loop(决策核心):思考→调用工具→观察→再决定」。
关键认知:Harness **不参与**"调哪个工具"的决策(那是大脑的事),只在决策外圈做**拦截、校验、重试、记录**。大脑可以是概率的、会犯错的;护甲必须是确定的、不出错的——「概率系统嫁接在确定性工程上」最直接的体现。

### 02 六子系统 × 三问归类
| 问题 | 属性 | 子系统 |
|---|---|---|
| 问题一:怎么不崩? | 可靠性·出错也能自己缓过来 | #1 错误分类与恢复、#2 迭代控制、#3 上下文工程 |
| 问题二:怎么不闯祸? | 安全性·危险动作有闸门 | #4 工具治理、#5 安全防护 |
| 问题三:怎么看得见? | 可控性·出了事查得到、复得了盘 | #6 可观测性 |

**#1 错误分类与恢复(Error Handling & Recovery)·不崩**
- 挡的风险:工具返回格式错、超时、接口偶发 5xx——任一未处理异常都让任务当场中断
- 做什么:错误分级(可重试=超时 / 可纠正=格式错 / 必须中止=权限拒绝);可重试自动退避重试;格式错回喂让模型自己改;真不行优雅降级而非抛栈崩掉
- 映射:手册·容错重试机制

**#2 迭代控制(Iteration / Loop Control)·不崩**
- 挡的风险:死循环反复调同一失败工具,烧 token 永不结束——一次失控可能是一笔可观账单
- 做什么:最大迭代步数 / 超时 / token 预算上限,到顶强制收尾;检测重复动作(同样调用连续出现就打断);触顶给「已尽力」兜底答复而非空转
- 映射:手册·主链路的步数/预算约束

**#3 上下文工程(Context Engineering)·不崩**
- 挡的风险:对话越长超窗→报错或关键信息被挤掉,「越聊越笨」答非所问
- 做什么:历史摘要、分层、虚拟分页,把窗口管在安全水位;每步只喂「这一步需要的」信息降噪;结构化压缩而非简单截断丢尾巴
- 映射:手册·记忆管理 #10(短期上下文)

**#4 工具治理(Tool Governance)·不闯祸**
- 挡的风险:模型决定调高危工具(删库、扣款、改配置),照单执行则一次幻觉 = 一次生产事故
- 做什么:工具分级 + 权限校验(模型只能调被授权的);高危操作先 dry-run / 二次确认;参数校验把关挡越界调用
- 映射:手册·Tool Governance #8(受控工具)

**#5 安全防护(Safety & Human Gate)·不闯祸**
- 挡的风险:提示注入、越权数据访问、敏感信息外泄;不可逆动作被自动执行后无法挽回
- 做什么:输入/输出过滤(识别注入与敏感数据);高风险不可逆动作设**人工审核闸门(Human-in-the-Loop)**;权限沙箱限定可碰的数据与系统边界
- 映射:手册·人工审核发布 #12

**#6 可观测性(Observability)·看得见**
- 挡的风险:线上 badcase 不知道哪一步想错、为什么这么干——无法复盘就无法改进,问题反复出现
- 做什么:全链路轨迹录制(每步思考、调用、结果留痕);成功率、耗时、token 指标监控;badcase 可回放、可归因,且**不落敏感信息**
- 映射:手册·Runtime Logs / Audit #11

### 03 评审检查清单(六问,没答全别说"能上线")
1. 工具调用失败会重试/纠正/降级而非直接崩?(#1)问开发:格式错、超时、5xx 分别怎么处理?
2. 有没有最大步数/token 预算上限防死循环烧钱?(#2)问开发:一个任务最坏花多少 token?触顶怎么收场?
3. 长对话会不会超窗变笨?(#3)问开发:聊到第 50 轮还准不准?靠摘要还是截断?
4. 高危工具(删除/扣款/改配置)有没有权限分级和执行前关卡?(#4)问开发:模型能直接调的工具清单?哪些要 dry-run?
5. 不可逆动作有没有人工审核闸门?能挡提示注入吗?(#5)问开发:哪些动作必须人点确认?敏感数据怎么隔离?
6. 线上 badcase 能不能回放、归因?日志会不会存敏感信息?(#6)问开发:出问题多久能定位到哪一步想错?

### 04 优先级
- **上线前必须有(红线):#4 工具治理 + #5 安全防护 + #6 可观测性**——管「不闯祸」和「出事查得到」;缺了它们一次幻觉就可能是真实事故且无从追责,是企业敢按发布键的底线
- **可随流量迭代(体验线):#1 错误恢复 + #2 迭代控制 + #3 上下文工程**——管「稳和省」,影响成功率/成本/长对话体验;初版做基础版,按真实 badcase 持续打磨;「观测先行,数据驱动迭代」

## 关键细节
- 量化:LLM 工具调用格式错误率 5–10%;检查清单第 3 问以"第 50 轮"为基准
- 错误三分法:可重试(超时)/ 可纠正(格式错)/ 必须中止(权限拒绝)
- 与手册模块映射:#1→容错重试,#2→主链路步数/预算约束,#3→Memory #10,#4→Tool Governance #8,#5→人工审核 #12,#6→Audit #11

## 洞见
- 本图把 Harness 从演进图的一张卡放大成可执行的**验收工具**:六问清单每条都附"问开发什么",是 PM 评审话术模板
- 红线/体验线的二分是本图独有的优先级判断:安全可控类(4/5/6)上线即需,可靠性类(1/2/3)允许迭代——与"观测比开发更重要"忠告一致,但注意可观测性被划入红线而非迭代项
- "护甲必须确定、大脑可以概率"是 deterministic-picker 思想在运行时层的表达
